# Versao do n8n
# Use "latest" para sempre pegar a mais recente, ou fixe uma versao especifica
# Dica: em producao, prefira versoes fixas para evitar quebras inesperadas
N8N_VERSION=latest

# Troque por uma string aleatoria com 32+ caracteres (letras e numeros)
N8N_ENCRYPTION_KEY=a7f3b2c1d4e5f6a7b8c9d0e1f2a3b4c5

# IP local para bind da porta do host
# Exemplos:
#   BIND_IP=0.0.0.0      -> Bind em todas as interfaces
#   BIND_IP=127.0.0.1    -> Bind apenas localhost
BIND_IP=127.0.0.1

# Porta exposta no host
BIND_PORT=5678

# Host/porta usados pelo n8n para gerar URLs
# Exemplos validos: localhost || n8n.lan || n8n.seudominio.com
N8N_HOST=localhost

# Porta do n8n
N8N_PORT=5678

# Protocolo do n8n
N8N_PROTOCOL=http

# URL do webhook
WEBHOOK_URL=http://localhost:5678/

# Fuso horario
GENERIC_TIMEZONE=America/Sao_Paulo
TZ=America/Sao_Paulo

# --- Banco de Dados (Postgres) ---
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n
POSTGRES_DB=n8n

# --- Redis ---
# Necessario para mode queue, opcional para single instance (cache)
# Mas como a stack eh postgres-redis, deixamos configurado caso queira ativar
# Se nao usar queue, o n8n ignora as configs de redis geralmente, a menos que ative cache
